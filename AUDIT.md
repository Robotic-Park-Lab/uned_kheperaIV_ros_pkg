# Audit — uned_kheperaIV_ros_pkg (2026-08-22)

Checklist grounded in real inspection of `humble-dev` (not guessed from memory): `colcon build`/`colcon test` of the 4 `ament_python` packages, `git log`/`git ls-tree` across `humble-dev` and `benchmark`, and reading the actual source of every package touched below. Following the same discipline used on `uned_crazyflie_ros_pkg`'s audit: this one folds in, from the start, both the "clean up what's broken" pass (equivalent to that repo's Fase 1) **and** the "make it English-documented, tested, and modular" pass (equivalent to its Fase 2), since that's what you asked for this time.

`benchmark` is **locked and out of scope for any code change** — it backs a published book chapter (same hard rule as `uned_crazyflie_ros_pkg`). Everything below only touches `humble-dev` (and `doc` for this document itself). Where `benchmark` is relevant, it's only read from, never written to.

Mark items `[x]` as they're resolved. Work started 2026-08-22 on branch `refactor/modular-restructuring`, after Francisco reviewed this document and gave the go-ahead for everything below (with the clarifications recorded inline), same review-before-merge pattern as the Crazyflie repo — this branch is not yet merged to `humble-dev`.

## 0 — Blocking decision: `libkhepera-2.1` licensing — RESOLVED

You asked to add `libkhepera-2.1` (used to build the C program that runs on the Khepera's onboard Linux, `scripts/server.c`) to the repo "si es lícito". Checked the actual library folder you left in this session's working directory: **no `LICENSE`/`COPYING` file anywhere in it**, and every source file header reads `Copyright (C) 2013 K-TEAM SA` with no redistribution grant of any kind. This repo is **public** and BSD-3-Clause licensed. **Decision (confirmed by Francisco)**: don't vendor the source. Resolved in `981975c`: `scripts/Makefile` builds `server.c`/`prog-template.c` against `libkhepera` expecting it at a documented `LIBKHEPERA_PATH` env var, without the vendor source itself living in this repo — verified for real, both `.c` files compile clean against Francisco's actual `libkhepera-2.1` headers (final ARM linking not tested, that build is ARM-only and this sandbox is x86_64).

## 0b — Urgent, found while working: leaked WiFi credential — RESOLVED

Not part of the original audit — found while reviewing `scripts/` for the Makefile above. A real WiFi password (`MOVISTAR_D647`) was hardcoded in plaintext in 3 files (`scripts/arduino-khepera-uROS/src/main.cpp`, `scripts/Arduino/test_ros2/test_ros2.ino`, `scripts/Arduino/test_wifi/test_wifi.ino`), committed several commits deep, in this public repo. Flagged to Francisco immediately rather than waiting. **Francisco confirmed the network is old/retired** — no git-history purge requested, just a clean `HEAD` going forward. Resolved in `981975c`: replaced with `SECRET_SSID`/`SECRET_PASS` from a gitignored `arduino_secrets.h` (same pattern already used in `test_wifi/ConnectNoEncryption/`), `.h.example` templates committed instead, `arduino_secrets.h` added to `.gitignore`.

## 1 — Metadata cleanup (all 8 packages) — RESOLVED (`e68dcb4`)

- [x] **`TODO: License declaration`** filled in all 8 `package.xml` → `BSD-3-Clause`.
- [x] **`TODO: Package description`** filled in all 8 with a real, specific description.
- [x] **Maintainer inconsistency** unified to `Francisco José Mañas Álvarez <fma527@ual.es>` across all 8 `package.xml`.
- [x] **Stray 0-byte file** `uned_kheperaiv_gui/uned_kheperaiv_gui/sys` removed.
- [x] **`uned_kheperaiv_webots` marker warning** fixed: split `resource/` (now only the ament marker) from a new `resources/` (the real data files: `default_config.yaml`, urdf files, `ros2control.yml`) — verified gone with a real `colcon build`. Also found while doing this: those `resources/` files look unreferenced anywhere in the repo (real robot description flows through `uned_kheperaiv_config/urdf/khepera.xml` instead) — not deleted without your confirmation, flagged here as **pending your review**.

## 2 — Real bugs and dependency gaps — RESOLVED (`e68dcb4`)

- [x] **Real bug in `experience.launch.py`**: `for robot_id in physical_khepera_list:` iterated a comma-joined string character by character. Fixed: `physical_khepera_list` is now a real Python list, verified with a synthetic 2-physical-robot experience producing exactly 2 driver nodes (was one broken node per character before).
- [x] **Two more real bugs found while verifying the fix above by actually running `get_ros2_nodes()` against all 3 existing experience `.yaml`, not just reading the code**: `Architecture.node['file']` and `Robots[x]['camera']` were read unconditionally, crashing with `KeyError` on `Demo_formation_central_webots.yaml` (missing both keys). Both now use `.get()` with a sensible fallback (the experience file itself; `'false'`) — verified, all 3 existing experiences now generate their launch actions without error.
- [x] **Undeclared dependencies**: real `<depend>`/`<exec_depend>` added to all packages missing them, including `multi_agent_pkg` in `uned_kheperaiv_webots`.
- [x] **Lint tests failing**: RESOLVED for `uned_kheperaiv_config`, `uned_kheperaiv_controllers`, `uned_kheperaiv_driver`, `uned_kheperaiv_gazebo`, `uned_kheperaiv_gui`, `uned_kheperaiv_task` — all 6 pass `colcon test` clean now (verified with a real, sequential `colcon test` run across the whole workspace, not just per-package). `uned_kheperaiv_webots` (3 pre-existing failures) and `uned_vicon_gazebo` (2) still have lint debt — see section 4.

## 3 — Duplicated / dead / unclear code — RESOLVED

- [x] **`uned_kheperaiv_gui`** dual-implementation pattern (commit `52d7e1b`): same fix as `uned_crazyflie_gui` — `interface_gui.py` fixed (package-relative imports, real `main()`), wired as the `interface_node` entry point, the `interface_node.py` stub removed. The original `RQT_Panel` X11-embedding vision dropped in favor of documenting it as future work (same tradeoff as Crazyflie). Real packaging bug found and fixed along the way: `main.ui`/`figs/*.png` were never installed outside `--symlink-install` mode.
- [x] **`uned_kheperaiv_task`** 3-way duplication (commit `fb50d84`): the marker-drawing half of `Agent.gtpose_callback` (identical math/color-thresholds across `distance_based_formation_control.py`/`gazebo_driver.py`) extracted to `formation_marker.py`; `gazebo_driver.py`'s `PIDController` extracted to `pid_controller.py`. The 3 `Agent`/`KheperaIVDriver` classes themselves deliberately **not** unified — read in full, they're 3 genuinely different formation strategies (different constructors, different subscribed message types, and `gazebo_driver.py` has an entirely different control architecture — internal PID + direct `cmd_vel`, vs. the other two which only publish `goal_pose`). `gazebo_driver.py`'s behavior preserved exactly, per the Gazebo-is-being-updated-not-retired decision.
- [x] **`uned_kheperaiv_controllers` missing from `humble-dev`**: ported from `benchmark` in `b26d050` (read-only, `benchmark` untouched).
- [x] **`turtlebot3burger_driver.py`**: removed, per your confirmation.
- [x] **Gazebo vs. Webots — decision executed**: `uned_khepera_description` merged into `uned_kheperaiv_config` (`75acf87`); `uned_khepera_gazebo` renamed to `uned_kheperaiv_gazebo` (also `75acf87`). Gazebo Classic 11 compatibility work done in `4a76577` — see section 4, **pending your validation**, not a plain "resolved".

## 4 — The broader pass (English docs, tests, driver consolidation, Gazebo update) — DONE, one item pending your validation

- [x] **`uned_kheperaiv_driver` + `uned_kheperaiv_webots`** (commit `250494e`): `PIDController` extracted to `uned_kheperaiv_driver/pid_controller.py`, verified byte-for-byte identical between the physical and Webots drivers before extracting. **Cross-repo dependency broken**: `khepera_driver.py` used to import `Agent` (genuinely used, 5 call sites, formation tracking) and `Crazyflie_ROS2` (dead import, never used) from **the Crazyflie repo** — `uned_kheperaiv_webots` needed `uned_crazyflie_ros_pkg` cloned and built in the same workspace just for formation control. Per your explicit confirmation: Khepera now has its own `agent.py` (adapted, not copied verbatim — the flight-specific `high_level_commander`/`scf` branches that don't apply to a ground robot were dropped). `kheperaIV_client_driver.py`'s own `Agent` (real TCP protocol, `"n"`/`"m"` commands to `scripts/server.c`) deliberately kept separate — different real protocol, not safe to merge blindly. 11 new tests (`test_pid_controller.py`, `test_agent.py`), both packages' READMEs written in English from scratch.
- [x] **`uned_kheperaiv_gazebo`: Gazebo Classic 11 update** (commit `4a76577`) — **implemented entirely by Claude, explicitly flagged as such in `uned_kheperaiv_gazebo/README.md`, not yet validated by you.** Verified for real with a headless `gzserver`: spawned all 4 Khepera IVs from `Demo_formation_webots.yaml`, published `cmd_vel`, confirmed real motion (position changed measurably, not just a topic existing) — independently reproduced in this session too (position.x moved from ~0 to 1.6m after 3s at 0.2 m/s). Fixed: `multiple_robot_Gazebo.launch.py`/`demo_formation_Gazebo.launch.py` pointed at non-existent yaml/rviz files and used a pose format mismatch; a new `uned_kheperaiv_config/resources/khepera_gazebo_default.yaml` provides the per-robot config `gazebo_driver.py` actually reads (didn't exist before). **Left broken, not fixed, and documented as such**: `simple_launch.py`'s `spawn_entity.py -database khepera_IV` approach — `uned_kheperaiv_config/model/model.config` names the model `Khepera-IV` (capital, hyphen) not `khepera_IV`, and `GAZEBO_MODEL_PATH` isn't set anywhere; redundant with the now-working URDF path, not worth maintaining two model-loading approaches. Formation behavior itself (`task.enable`) left `false` in the new default config — the real inter-robot distances/relationships were never specified anywhere in the repo and weren't safe to invent.
- [x] **Lint cleanup pass** (commit `4bb65e1`, done directly after the 4 parallel passes landed, found by running `colcon test` on the whole workspace for the first time): `uned_kheperaiv_config` (85 failures → 0), `uned_kheperaiv_driver`'s `kheperaIV_client_driver.py` (207 → 0), `uned_kheperaiv_gazebo` (60 → 0). Two real bugs found and fixed along the way, not just style: `urdf_visualize.launch.py` built a `robot_joint_state_publisher_node` but never added it to the returned `LaunchDescription` (joint states never actually published) — now included. A suspicious `"i"` (goal) command in `kheperaIV_client_driver.py` sends `msg.pose.position.x` twice instead of x/y — **not silently changed**, flagged with a comment, needs checking against the real `server.c` wire protocol on actual hardware before touching.
- [x] **`experience.launch.py` bugs**: fixed in section 2, all 3 existing experience `.yaml` (plus a synthetic physical-robot one) verified against it with a real `LaunchContext`.
- [x] Root README, `scripts/README.md`, `uned_kheperaiv_config/README.md`, `uned_kheperaiv_controllers/README.md` written in English from scratch.
- [ ] **`scripts/` review**: `Makefile` done (section 0), credential leak fixed (section 0b). Arduino/ESP32 micro-ROS prototypes and the 2 Matlab files still need your manual "keep vs. retire" call.

## Final state (2026-08-22) — everything on `refactor/modular-restructuring`, not yet merged to `humble-dev`

Verified with a real, sequential `colcon build` + `colcon test` of the whole workspace: **8/8 packages build clean.** 6/8 (`uned_kheperaiv_config`, `uned_kheperaiv_controllers`, `uned_kheperaiv_driver`, `uned_kheperaiv_gazebo`, `uned_kheperaiv_gui`, `uned_kheperaiv_task`) pass their lint tests clean. Real functional tests added and passing in `uned_kheperaiv_driver`, `uned_kheperaiv_webots`, `uned_kheperaiv_task`, `uned_kheperaiv_gui` (headless smoke test), `uned_kheperaiv_controllers` still has none (flagged in its own README as follow-up).

**What's left for you specifically, before merging to `humble-dev`:**
1. **Validate the Gazebo Classic 11 update** (`uned_kheperaiv_gazebo`) on your own setup — it's real-verified in this sandbox but explicitly marked as AI-authored and unconfirmed by you.
2. **Decide** on the unreferenced `uned_kheperaiv_webots/resources/` files (kheperaiv.urdf, kheperaiv_IPC.urdf, kheperaiv_ForceField.urdf, default_config.yaml, ros2control.yml) — look dead, not deleted without your confirmation.
3. **Check** the suspicious `"i"` command x/x duplication in `kheperaIV_client_driver.py` (flagged with a comment, not changed) against the real `server.c` protocol.
4. **Clean up** `uned_kheperaiv_webots`'s and `uned_vicon_gazebo`'s remaining lint debt, if you want it — both were flagged as pre-existing/out-of-scope rather than fixed blind.
5. **Review manually**: `scripts/`'s Arduino/ESP32/Matlab files (keep vs. retire).
6. Add tests to `uned_kheperaiv_controllers` if you want the same coverage the other packages got.

## How this maps to the Crazyflie repo's phases, for reference

Section 1 ≈ Crazyflie's Fase 1 "Simples". Section 2 ≈ Fase 1 "Medias". Section 3 ≈ Fase 1 "Complejas" + the missing-controllers-package finding (no Crazyflie equivalent — that repo never lost a package between branches). Section 4 ≈ Crazyflie's Fase 2, done as one pass instead of two this time, per your request.
