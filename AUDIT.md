# Auditoría — uned_kheperaIV_ros_pkg (2026-08-22)

Checklist fundamentada en inspección real de `humble-dev` (no deducida de memoria): `colcon build`/`colcon test` de los 4 paquetes `ament_python`, `git log`/`git ls-tree` a través de `humble-dev` y `benchmark`, y lectura del código fuente real de cada paquete tocado abajo. Siguiendo la misma disciplina usada en la auditoría de `uned_crazyflie_ros_pkg`: esta incorpora desde el principio tanto la pasada de "limpiar lo que está roto" (equivalente a la Fase 1 de ese repositorio) **como** la de "documentarlo en inglés, testearlo y modularizarlo" (equivalente a su Fase 2), ya que eso es lo que pediste esta vez.

`benchmark` está **bloqueada y fuera de alcance para cualquier cambio de código** — respalda un capítulo de libro publicado (la misma regla estricta que `uned_crazyflie_ros_pkg`). Todo lo de abajo solo toca `humble-dev` (y `doc` para este propio documento). Donde `benchmark` es relevante, solo se lee, nunca se escribe.

Marca los puntos con `[x]` conforme se resuelven. El trabajo empezó el 2026-08-22 en la rama `refactor/modular-restructuring`, después de que Francisco revisara este documento y diera el visto bueno a todo lo de abajo (con las aclaraciones anotadas en línea), el mismo patrón de revisión-antes-de-fusionar que el repositorio de Crazyflie — esta rama todavía no está fusionada a `humble-dev`.

## 0 — Decisión bloqueante: licencia de `libkhepera-2.1` — RESUELTO

Pediste añadir `libkhepera-2.1` (usado para compilar el programa en C que corre en el Linux embebido del Khepera, `scripts/server.c`) al repositorio "si es lícito". Se comprobó la carpeta real de la librería que dejaste en el directorio de trabajo de esta sesión: **no hay ningún fichero `LICENSE`/`COPYING` en ningún sitio de ella**, y cada cabecera de fichero fuente dice `Copyright (C) 2013 K-TEAM SA` sin ningún tipo de concesión de redistribución. Este repositorio es **público** y tiene licencia BSD-3-Clause. **Decisión (confirmada por Francisco)**: no incluir el código fuente. Resuelto en `981975c`: `scripts/Makefile` compila `server.c`/`prog-template.c` contra `libkhepera` esperándola en una variable de entorno documentada `LIBKHEPERA_PATH`, sin que el propio código fuente del proveedor viva en este repositorio — verificado de verdad, ambos ficheros `.c` compilan limpio contra las cabeceras reales de `libkhepera-2.1` de Francisco (el enlazado final para ARM no se probó, esa compilación es solo ARM y este sandbox es x86_64).

## 0b — Urgente, encontrado mientras se trabajaba: credencial WiFi filtrada — RESUELTO

No formaba parte de la auditoría original — encontrado al revisar `scripts/` para el Makefile de arriba. Una contraseña WiFi real (`MOVISTAR_D647`) estaba escrita en texto plano en 3 ficheros (`scripts/arduino-khepera-uROS/src/main.cpp`, `scripts/Arduino/test_ros2/test_ros2.ino`, `scripts/Arduino/test_wifi/test_wifi.ino`), commiteada varios commits atrás, en este repositorio público. Señalado a Francisco de inmediato en vez de esperar. **Francisco confirmó que la red es antigua/dada de baja** — no pidió purgar el historial de git, solo un `HEAD` limpio de aquí en adelante. Resuelto en `981975c`: sustituido por `SECRET_SSID`/`SECRET_PASS` desde un `arduino_secrets.h` ignorado por git (mismo patrón ya usado en `test_wifi/ConnectNoEncryption/`), plantillas `.h.example` commiteadas en su lugar, `arduino_secrets.h` añadido a `.gitignore`.

## 1 — Limpieza de metadatos (los 8 paquetes) — RESUELTO (`e68dcb4`)

- [x] **`TODO: License declaration`** rellenado en los 8 `package.xml` → `BSD-3-Clause`.
- [x] **`TODO: Package description`** rellenado en los 8 con una descripción real y específica.
- [x] **Inconsistencia de maintainer** unificada a `Francisco José Mañas Álvarez <fma527@ual.es>` en los 8 `package.xml`.
- [x] **Fichero suelto de 0 bytes** `uned_kheperaiv_gui/uned_kheperaiv_gui/sys` eliminado.
- [x] **Aviso de marker de `uned_kheperaiv_webots`** corregido: se dividió `resource/` (ahora solo el marker de ament) de un nuevo `resources/` (los ficheros de datos reales: `default_config.yaml`, ficheros urdf, `ros2control.yml`) — verificado que desaparece con un `colcon build` real. También encontrado al hacer esto: esos ficheros de `resources/` parecen no estar referenciados en ningún sitio del repositorio (la descripción real del robot fluye en su lugar por `uned_kheperaiv_config/urdf/khepera.xml`) — no eliminados sin tu confirmación, señalado aquí como **pendiente de tu revisión**.

## 2 — Bugs reales y huecos de dependencias — RESUELTO (`e68dcb4`)

- [x] **Bug real en `experience.launch.py`**: `for robot_id in physical_khepera_list:` iteraba carácter a carácter sobre una cadena unida por comas. Corregido: `physical_khepera_list` es ahora una lista Python real, verificado con una experiencia sintética de 2 robots físicos produciendo exactamente 2 nodos driver (antes era un nodo roto por cada carácter).
- [x] **Dos bugs reales más encontrados al verificar la corrección de arriba ejecutando de verdad `get_ros2_nodes()` contra los 3 `.yaml` de experiencia existentes, no solo leyendo el código**: `Architecture.node['file']` y `Robots[x]['camera']` se leían sin condición, fallando con `KeyError` en `Demo_formation_central_webots.yaml` (le faltaban ambas claves). Ambos usan ahora `.get()` con un valor por defecto razonable (el propio fichero de experiencia; `'false'`) — verificado, las 3 experiencias existentes generan ahora sus acciones de lanzamiento sin error.
- [x] **Dependencias no declaradas**: `<depend>`/`<exec_depend>` reales añadidos a todos los paquetes a los que les faltaban, incluyendo `multi_agent_pkg` en `uned_kheperaiv_webots`.
- [x] **Tests de lint en rojo**: RESUELTO para `uned_kheperaiv_config`, `uned_kheperaiv_controllers`, `uned_kheperaiv_driver`, `uned_kheperaiv_gazebo`, `uned_kheperaiv_gui`, `uned_kheperaiv_task` — los 6 pasan `colcon test` limpio ahora (verificado con una ejecución real y secuencial de `colcon test` de todo el workspace, no solo por paquete). `uned_kheperaiv_webots` (3 fallos preexistentes) y `uned_vicon_gazebo` (2) todavía tienen deuda de lint — ver sección 4.

## 3 — Código duplicado / muerto / poco claro — RESUELTO

- [x] **`uned_kheperaiv_gui`** patrón de doble implementación (commit `52d7e1b`): misma corrección que `uned_crazyflie_gui` — `interface_gui.py` corregido (imports relativos al paquete, `main()` real), conectado como el punto de entrada `interface_node`, el stub `interface_node.py` eliminado. La visión original de `RQT_Panel` embebido vía X11 se descartó en favor de documentarla como trabajo futuro (misma decisión que en Crazyflie). Bug real de empaquetado encontrado y corregido de paso: `main.ui`/`figs/*.png` nunca se instalaban fuera del modo `--symlink-install`.
- [x] **`uned_kheperaiv_task`** duplicación triple (commit `fb50d84`): la mitad de dibujo de marcador de `Agent.gtpose_callback` (matemáticas/umbrales de color idénticos en `distance_based_formation_control.py`/`gazebo_driver.py`) extraída a `formation_marker.py`; el `PIDController` de `gazebo_driver.py` extraído a `pid_controller.py`. Las 3 clases `Agent`/`KheperaIVDriver` en sí deliberadamente **no** se unificaron — leídas por completo, son 3 estrategias de formación genuinamente distintas (constructores distintos, tipos de mensaje suscritos distintos, y `gazebo_driver.py` tiene una arquitectura de control completamente distinta — PID interno + `cmd_vel` directo, frente a los otros dos que solo publican `goal_pose`). El comportamiento de `gazebo_driver.py` se preservó exactamente, según la decisión de que Gazebo se está actualizando, no retirando.
- [x] **`uned_kheperaiv_controllers` ausente de `humble-dev`**: portado desde `benchmark` en `b26d050` (solo lectura, `benchmark` sin tocar).
- [x] **`turtlebot3burger_driver.py`**: eliminado, según tu confirmación.
- [x] **Gazebo frente a Webots — decisión ejecutada**: `uned_khepera_description` fusionado en `uned_kheperaiv_config` (`75acf87`); `uned_khepera_gazebo` renombrado a `uned_kheperaiv_gazebo` (también `75acf87`). Trabajo de compatibilidad con Gazebo Classic 11 hecho en `4a76577` — ver sección 4, **pendiente de tu validación**, no un simple "resuelto".

## 4 — La pasada más amplia (documentación en inglés, tests, consolidación de drivers, actualización de Gazebo) — HECHO, un punto pendiente de tu validación

- [x] **`uned_kheperaiv_driver` + `uned_kheperaiv_webots`** (commit `250494e`): `PIDController` extraído a `uned_kheperaiv_driver/pid_controller.py`, verificado idéntico byte a byte entre el driver físico y el de Webots antes de extraerlo. **Dependencia entre repositorios rota**: `khepera_driver.py` solía importar `Agent` (usada de verdad, 5 puntos de llamada, seguimiento de formación) y `Crazyflie_ROS2` (import muerto, nunca usado) desde **el repositorio de Crazyflie** — `uned_kheperaiv_webots` necesitaba tener `uned_crazyflie_ros_pkg` clonado y compilado en el mismo workspace solo para el control de formación. Según tu confirmación explícita: Khepera tiene ahora su propio `agent.py` (adaptado, no copiado literalmente — las ramas específicas de vuelo `high_level_commander`/`scf`, que no aplican a un robot terrestre, se eliminaron). La propia `Agent` de `kheperaIV_client_driver.py` (protocolo TCP real, comandos `"n"`/`"m"` a `scripts/server.c`) se mantuvo deliberadamente separada — protocolo real distinto, no seguro fusionarlo a ciegas. 11 tests nuevos (`test_pid_controller.py`, `test_agent.py`), READMEs de ambos paquetes escritos en inglés desde cero.
- [x] **`uned_kheperaiv_gazebo`: actualización a Gazebo Classic 11** (commit `4a76577`) — **implementado por completo por Claude, señalado explícitamente como tal en `uned_kheperaiv_gazebo/README.md`, todavía no validado por ti.** Verificado de verdad con un `gzserver` headless: se hizo spawn de los 4 Khepera IV de `Demo_formation_webots.yaml`, se publicó `cmd_vel`, se confirmó movimiento real (la posición cambió de forma medible, no solo la existencia de un topic) — reproducido de forma independiente también en esta sesión (position.x se movió de ~0 a 1.6m tras 3s a 0.2 m/s). Corregido: `multiple_robot_Gazebo.launch.py`/`demo_formation_Gazebo.launch.py` apuntaban a ficheros yaml/rviz inexistentes y usaban un formato de pose distinto; un nuevo `uned_kheperaiv_config/resources/khepera_gazebo_default.yaml` aporta la configuración por robot que `gazebo_driver.py` realmente lee (no existía antes). **Dejado roto, no corregido, y documentado como tal**: el enfoque de `simple_launch.py` con `spawn_entity.py -database khepera_IV` — `uned_kheperaiv_config/model/model.config` nombra el modelo `Khepera-IV` (con mayúscula y guion) no `khepera_IV`, y `GAZEBO_MODEL_PATH` no está definida en ningún sitio; redundante con la ruta URDF ya funcional, no merece la pena mantener dos enfoques de carga de modelo. El propio comportamiento de formación (`task.enable`) se dejó en `false` en la nueva configuración por defecto — las distancias/relaciones reales entre robots nunca se especificaron en ningún sitio del repositorio y no era seguro inventarlas.
- [x] **Pasada de limpieza de lint** (commit `4bb65e1`, hecha justo después de que aterrizaran las 4 pasadas paralelas, encontrada al ejecutar `colcon test` de todo el workspace por primera vez): `uned_kheperaiv_config` (85 fallos → 0), el `kheperaIV_client_driver.py` de `uned_kheperaiv_driver` (207 → 0), `uned_kheperaiv_gazebo` (60 → 0). Dos bugs reales encontrados y corregidos de paso, no solo de estilo: `urdf_visualize.launch.py` construía un `robot_joint_state_publisher_node` pero nunca lo añadía al `LaunchDescription` devuelto (los estados de las articulaciones nunca se publicaban de verdad) — ahora incluido. Un sospechoso comando `"i"` (goal) en `kheperaIV_client_driver.py` envía `msg.pose.position.x` dos veces en vez de x/y — **no cambiado silenciosamente**, señalado con un comentario, necesita comprobarse contra el protocolo de cable real de `server.c` en hardware real antes de tocarlo.
- [x] **Bugs de `experience.launch.py`**: corregidos en la sección 2, los 3 `.yaml` de experiencia existentes (más uno sintético de robot físico) verificados contra ella con un `LaunchContext` real.
- [x] README raíz, `scripts/README.md`, `uned_kheperaiv_config/README.md`, `uned_kheperaiv_controllers/README.md` escritos en inglés desde cero.
- [ ] **Revisión de `scripts/`**: `Makefile` hecho (sección 0), filtración de credencial corregida (sección 0b). Los prototipos micro-ROS de Arduino/ESP32 y los 2 ficheros de Matlab todavía necesitan tu decisión manual de "conservar vs. retirar".

## Estado final (2026-08-22) — todo en `refactor/modular-restructuring`, todavía sin fusionar a `humble-dev`

Verificado con un `colcon build` + `colcon test` real y secuencial de todo el workspace: **8/8 paquetes compilan limpio.** 6/8 (`uned_kheperaiv_config`, `uned_kheperaiv_controllers`, `uned_kheperaiv_driver`, `uned_kheperaiv_gazebo`, `uned_kheperaiv_gui`, `uned_kheperaiv_task`) pasan sus tests de lint limpio. Tests funcionales reales añadidos y pasando en `uned_kheperaiv_driver`, `uned_kheperaiv_webots`, `uned_kheperaiv_task`, `uned_kheperaiv_gui` (smoke test headless), `uned_kheperaiv_controllers` todavía no tiene ninguno (señalado en su propio README como trabajo futuro).

**Lo que queda específicamente en tus manos, antes de fusionar a `humble-dev`:**
1. **Validar la actualización a Gazebo Classic 11** (`uned_kheperaiv_gazebo`) en tu propio entorno — está verificada de verdad en este sandbox pero explícitamente marcada como escrita por IA y sin confirmar por ti.
2. **Decidir** sobre los ficheros no referenciados de `uned_kheperaiv_webots/resources/` (kheperaiv.urdf, kheperaiv_IPC.urdf, kheperaiv_ForceField.urdf, default_config.yaml, ros2control.yml) — parecen muertos, no eliminados sin tu confirmación.
3. **Comprobar** la sospechosa duplicación x/x del comando `"i"` en `kheperaIV_client_driver.py` (señalada con un comentario, no cambiada) contra el protocolo real de `server.c`.
4. **Limpiar** la deuda de lint restante de `uned_kheperaiv_webots` y `uned_vicon_gazebo`, si quieres — ambas se señalaron como preexistentes/fuera de alcance en vez de corregirse a ciegas.
5. **Revisar manualmente**: los ficheros Arduino/ESP32/Matlab de `scripts/` (conservar vs. retirar).
6. Añadir tests a `uned_kheperaiv_controllers` si quieres la misma cobertura que recibieron los demás paquetes.

## Cómo se corresponde esto con las fases del repositorio de Crazyflie, como referencia

La sección 1 ≈ la Fase 1 "Simples" de Crazyflie. La sección 2 ≈ la Fase 1 "Medias". La sección 3 ≈ la Fase 1 "Complejas" + el hallazgo del paquete de controladores ausente (sin equivalente en Crazyflie — ese repositorio nunca perdió un paquete entre ramas). La sección 4 ≈ la Fase 2 de Crazyflie, hecha esta vez en una sola pasada en vez de dos, según tu petición.
